<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.wemeet.kele.mapper.SongMapper">
    <select id="getBySongmidAndSource" resultType="me.wemeet.kele.entity.Song">
        SELECT song.* FROM song WHERE songmid = #{songmid} AND source = #{source}
    </select>

</mapper>
